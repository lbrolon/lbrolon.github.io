<ul class="card-text font-weight-light list-group list-group-flush">
  {% assign entries = data[1].scientificInvolvement | sort: 'startDate' | reverse %}
  {% for content in entries %}
    <li class="list-group-item">
      <div class="row">
        <div class="col-xs-2 col-sm-2 col-md-2 text-center date-column">
          {% assign startYear = content.startDate | slice: 0, 4 %}
          {% assign endYear = content.endDate | slice: 0, 4 | default: 'Present' %}

          {% if startYear == endYear %}
            {% assign date = startYear %}
          {% else %}
            {% assign date = startYear | append: '–' | append: endYear %}
          {% endif %}

          <table class="table-cv">
            <tbody>
              <tr>
                <td>
                  <span class="badge font-weight-bold danger-color-dark text-uppercase align-middle" style="min-width: 75px">
                    {{ date }}
                  </span>
                </td>
              </tr>
            </tbody>
          </table>
        </div>

        <div class="col-xs-10 col-sm-10 col-md-10 mt-2 mt-md-0">
          <h6 class="ml-1 ml-md-4" style="font-size: 0.95rem; font-style: italic">
            {{ content.details }}
          </h6>
        </div>
      </div>
    </li>
  {% endfor %}
</ul>
